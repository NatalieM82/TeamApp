<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="New Text"
        android:id="@+id/teamName"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textSize="20dp"
        android:layout_toLeftOf="@+id/deleteTeamBtn"
        android:layout_alignBottom="@+id/deleteTeamBtn"
        android:gravity="center_vertical"
        android:textColor="#fff" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:id="@+id/deleteTeamBtn"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:background="#FB6668"
        android:textColor="#fff"
        android:onClick="onDoneButtonClick"/>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit"
        android:id="@+id/editTeamBtn"
        android:background="#1ab188"
        android:textColor="#fff"
        android:onClick="onDoneButtonClick"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@+id/deleteTeamBtn" />
</RelativeLayout>